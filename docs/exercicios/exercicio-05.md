# Exercícios - Aula 05: Layouts e Organização Visual 🎨

### 🟢 Básicos (Fixação)

1. **Eixos**: Em uma `Column`, qual a diferença entre `MainAxisAlignment` e `CrossAxisAlignment`?
2. **Caixas**: Para que serve o widget `Container`? Imagine 3 propriedades que você poderia mudar nele para destacar um título da tela.

### 🟡 Intermediários (Aplicação)

3. **Espaçamento**: Diferencie `Padding` de `Margin`. Se você quiser afastar um botão das bordas de um cartão, qual dos dois deve usar dentro do cartão?
4. **Expansão**: Para que serve o widget `Expanded`? O que acontece se colocarmos dois widgets `Expanded` dentro da mesma `Row`?

### 🔴 Desafio (Pesquisa/Prática)

5. **Responsividade**: Como você faria para que uma lista de itens aparecesse em 1 coluna no celular (vertical) mas em 3 colunas em um tablet (horizontal)? Pesquise sobre `LayoutBuilder` ou `MediaQuery`.