# Exercícios - Aula 13: Publicando para Android 🤖

### 🟢 Básicos (Fixação)

1. **Gradle**: Onde definimos o `versionName` e o `versionCode` do nosso aplicativo Android?
2. **Build**: Qual o comando usado para gerar um `App Bundle` (.aab) para a Google Play Store?

### 🟡 Intermediários (Aplicação)

3. **Assinatura**: O que é uma `Keystore` e por que ela é obrigatória para publicar um app na loja oficial? O que acontece se você perder esse arquivo?
4. **Permissões**: Cite duas permissões comuns que devem ser declaradas no `AndroidManifest.xml` (ex: Internet, Câmera).

### 🔴 Desafio (Pesquisa/Prática)

5. **Distribuição**: Pesquise sobre o "Internal App Sharing" da Google Play. Como ele ajuda no processo de teste antes de liberar o app para o público geral?